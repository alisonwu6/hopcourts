version: '3.8'
name: sportsmatch
services:
  api:
    build: .
    container_name: sportsmatch-backend
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      NODE_ENV: development
    command: npm run dev
